const add = (a: number, b: number): number => {
  return a + b;
};

const today = {
  date: new Date(),
  weather: 'sunny',
};

const logWeather = ({
  date,
  weather,
}: {
  date: Date;
  weather: string;
}): string => {
  return 'hello';
};


